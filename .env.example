# Database
DATABASE_URL="file:./dev.db"

# AI provider — leave empty to run with AI features clearly marked "not connected"
ANTHROPIC_API_KEY=

# Session signing secret — generate a real random value in production
AUTH_SECRET=
